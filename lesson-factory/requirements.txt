# P0 (계약 정의) · P1 (세안 생성) — 지금 필요한 것
jsonschema>=4.21
pytest>=8.0
python-dotenv>=1.0
python-docx>=1.1        # render/sean_docx.py — 없으면 세안 .docx 만 건너뛴다

# P2 이후 (오케스트레이터 · 에이전트)
anthropic>=0.40

# P2.5 이후 (텔레그램 승인 봇)
python-telegram-bot>=21.0

# P3 이후 (실파일 렌더링)
python-pptx>=1.0
